---
import DocsLayout from '../../layouts/DocsLayout.astro';
import EmbedComponent from '../../components/Embed.astro';

const usageSnippet = `---
import { Embed } from 'comet-astro';

---

<Embed
  src="https://www.youtube.com/embed/dQw4w9WgXcQ"
  title="YouTube Video"
  aspectRatio="16/9"
  allowFullscreen={true}
  loading="lazy"
/>`;
---

<DocsLayout
  seoTitle="Embed"
  seoDescription="Accessible iframe wrapper with WCAG AAA contrast, responsive aspect ratios, and proper focus management."
>
  <section class="space-y-4">
    <p class="text-base text-slate-100">
      Embed provides accessible iframe containers with comprehensive keyboard
      navigation and focus management. The component maintains WCAG AAA contrast
      ratios and supports multiple aspect ratios for responsive design across
      all device sizes.
    </p>
    <p class="text-base text-slate-200">
      Enhanced focus indicators, proper ARIA labeling, and sandbox security
      ensure safe embedding of external content while maintaining optimal
      accessibility for screen readers and keyboard navigation.
    </p>
  </section>

  <section class="mt-10 space-y-6">
    <h2 class="text-xl font-semibold text-slate-100">Props</h2>
    <dl class="grid gap-6 sm:grid-cols-2">
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          src
        </dt>
        <dd class="text-sm text-slate-200">
          URL of the content to embed. Required for iframe source.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          title?
        </dt>
        <dd class="text-sm text-slate-200">
          Accessible title for screen readers. Defaults to <code
            >"Embedded content"</code
          >.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          aspectRatio?
        </dt>
        <dd class="text-sm text-slate-200">
          Responsive aspect ratio: <code>"16/9"</code>, <code>"4/3"</code>, <code
            >"1/1"</code
          >, <code>"21/9"</code>, or <code>"none"</code>. Defaults to <code
            >"16/9"</code
          >.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          height?
        </dt>
        <dd class="text-sm text-slate-200">
          Fixed height in pixels when <code>aspectRatio="none"</code>. Overrides
          aspect ratio behavior.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          allowFullscreen?
        </dt>
        <dd class="text-sm text-slate-200">
          Enable fullscreen capability for embedded content. Defaults to <code
            >true</code
          >.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          loading?
        </dt>
        <dd class="text-sm text-slate-200">
          Loading behavior: <code>"lazy"</code> or <code>"eager"</code>.
          Defaults to <code>"lazy"</code>.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          sandbox?
        </dt>
        <dd class="text-sm text-slate-200">
          Sandbox restrictions for embedded content security. Optional security
          configuration.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          allow?
        </dt>
        <dd class="text-sm text-slate-200">
          Feature policy permissions for embedded content. Defaults include
          accelerometer, autoplay, clipboard access.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          ariaLabel?
        </dt>
        <dd class="text-sm text-slate-200">
          Additional ARIA label for enhanced screen reader support and context.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          ariaDescribedby?
        </dt>
        <dd class="text-sm text-slate-200">
          Reference to element that describes the embedded content for
          accessibility.
        </dd>
      </div>
      <div
        class="space-y-1 rounded-lg border border-slate-700/75 bg-slate-900/60 p-4 shadow-sm"
      >
        <dt
          class="text-sm font-semibold uppercase tracking-wide text-slate-200"
        >
          className?
        </dt>
        <dd class="text-sm text-slate-200">
          Additional CSS classes to apply to the embed wrapper for custom
          styling.
        </dd>
      </div>
    </dl>
  </section>

  <section class="mt-10 space-y-6">
    <h2 class="text-xl font-semibold text-slate-100">Usage</h2>
    <p class="text-base text-slate-200">
      Embed external content safely with responsive aspect ratios and
      accessibility features. The component automatically handles focus
      management and provides proper ARIA attributes for screen reader
      compatibility.
    </p>
    <pre
      class="overflow-x-auto rounded-lg border border-slate-700/70 bg-slate-950/80 p-4 text-sm text-slate-50 shadow-inner">
      <code class="language-astro">{usageSnippet}</code>
    </pre>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Video Embed (16:9)</h2>
    <p class="text-base text-slate-200">
      Standard video aspect ratio for YouTube, Vimeo, and most video content.
    </p>
    <div class="max-w-2xl">
      <EmbedComponent
        src="https://www.youtube.com/embed/dQw4w9WgXcQ"
        title="YouTube Video Example"
        aspectRatio="16/9"
        allowFullscreen={true}
        loading="lazy"
      />
    </div>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Square Embed (1:1)</h2>
    <p class="text-base text-slate-200">
      Perfect for Instagram posts, square widgets, or social media content.
    </p>
    <div class="max-w-md">
      <EmbedComponent
        src="https://codepen.io/team/codepen/embed/preview/PNaGbb?height=400"
        title="CodePen Square Example"
        aspectRatio="1/1"
        allowFullscreen={true}
      />
    </div>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Classic TV (4:3)</h2>
    <p class="text-base text-slate-200">
      Traditional television aspect ratio for older content or specific layouts.
    </p>
    <div class="max-w-xl">
      <EmbedComponent
        src="https://codepen.io/team/codepen/embed/preview/PNaGbb?height=400"
        title="Classic TV Aspect Ratio"
        aspectRatio="4/3"
        allowFullscreen={true}
      />
    </div>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Ultra-wide (21:9)</h2>
    <p class="text-base text-slate-200">
      Cinematic ultra-wide format for panoramic content and immersive
      experiences.
    </p>
    <div class="max-w-4xl">
      <EmbedComponent
        src="https://codepen.io/team/codepen/embed/preview/PNaGbb?height=400"
        title="Ultra-wide Cinematic View"
        aspectRatio="21/9"
        allowFullscreen={true}
      />
    </div>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Fixed Height</h2>
    <p class="text-base text-slate-200">
      Use fixed pixel height when aspect ratio constraints don't fit your
      content needs.
    </p>
    <div class="max-w-3xl">
      <EmbedComponent
        src="https://codepen.io/team/codepen/embed/preview/PNaGbb?height=400"
        title="Fixed Height Example"
        aspectRatio="none"
        height={300}
        allowFullscreen={true}
      />
    </div>
  </section>

  <section class="mt-10 space-y-8">
    <h2 class="text-xl font-semibold text-slate-100">Enhanced Accessibility</h2>
    <p class="text-base text-slate-200">
      Demonstrate additional ARIA attributes for complex embedded content.
    </p>
    <div class="max-w-2xl">
      <p id="embed-description" class="text-sm text-slate-300 mb-4">
        This interactive map shows real-time data visualization with keyboard
        navigation support.
      </p>
      <EmbedComponent
        src="https://codepen.io/team/codepen/embed/preview/PNaGbb?height=400"
        title="Interactive Data Visualization"
        aspectRatio="16/9"
        ariaLabel="Interactive data visualization map with real-time updates"
        ariaDescribedby="embed-description"
        allowFullscreen={true}
      />
    </div>
  </section>

  <section class="mt-10 space-y-4">
    <h2 class="text-xl font-semibold text-slate-100">Accessibility</h2>
    <p class="text-base text-slate-200">
      Contrast ratios exceed WCAG AAA requirements (7:1+) for borders and focus
      indicators in both light and dark themes. Enhanced focus rings use strong
      blue colors with proper offset spacing for clear keyboard navigation
      visibility.
    </p>
    <p class="text-base text-slate-200">
      Embedded iframes include proper <code>title</code> attributes and optional
      <code>aria-label</code> and <code>aria-describedby</code> for enhanced screen
      reader support. Keyboard focus management allows full navigation within embedded
      content using standard Tab navigation patterns.
    </p>
    <p class="text-base text-slate-200">
      Security features include configurable <code>sandbox</code> restrictions and
      <code>allow</code> policies for safe embedding of external content. Lazy loading
      is enabled by default to improve page performance while maintaining accessibility
      for users with slower connections.
    </p>
  </section>
</DocsLayout>
